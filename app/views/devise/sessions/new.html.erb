<div class="container">
  <div class="row">
    <div class="col-sm-8 col-md-7 hidden-xs-down bg-faded sidebar welcome">
      <h2 class="featurette-heading">Welcome to <span class="text-colored">Broken Vessel </span></h2>
      <p class="lead">“There are wounds that never show on the body that are deeper and more hurtful than anything that bleeds.” -Laurell K. Hamilton</p>
      <h4>Do you feel overwhelmed by your situation?</h4>
      <ul>
        <li>Get Paired with a volunteer listener</li>
        <li>If you prefer a qualified personnel, find one with us at a subsidised price</li>
        <li>Join our community and learn how they handled a similiar situation</li>
        <li>We promise to do our best to help you through it</li>
      </ul>
    </div>
    <div class="col-md-5 welcome-2">
      <div class="signin-wrapper">
        <h2>Log in to <span class="text-colored">continue</span></h2>

        <%= form_for(resource, as: resource_name, class: "form-signin", url: session_path(resource_name)) do |f| %>
          <div class="field">
            <%= f.label :email, class: "sr-only" %><br>
            <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control", placeholder: "Email address" %>
          </div>

          <div class="field">
            <%= f.label :password, class: "sr-only" %><br>
            <%= f.password_field :password, autocomplete: "current-password", class: "form-control", placeholder: "Password" %>
          </div>

          <% if devise_mapping.rememberable? %>
            <div class="checkbox">
              <%= f.check_box :remember_me %>
              <%= f.label :remember_me %>
            </div>
          <% end %>

          <div class="actions">
            <%= f.submit "Log in", class: "btn btn-lg btn-primary btn-block" %>
          </div>
        <% end %>

        <%= render "devise/shared/links" %>
      </div>
    </div>
  </div>
</div>
<%= render 'shared/footer'%>
